# Changelog

## 0.1.0b1

- Initial dependency-free Python G2 recorder and verifier.
- Exact `gradia.guard.sdk-bundle.v1` interoperability with `@gradia/guard`.
- Pre-dispatch policy censoring, exact model/tool identities, parent/retry
  lineage, digest-only content references, and crash-visible unfinalized state.
- Pinned LangChain Core callback cell for model and registered-tool activity.
- Account-free `doctor`, `verify`, and `inspect` commands.
